---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: inferenceservices.kubeai.platform.io
spec:
  group: kubeai.platform.io
  names:
    kind: InferenceService
    listKind: InferenceServiceList
    plural: inferenceservices
    shortNames:
    - isvc
    singular: inferenceservice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.modelName
      name: Model
      type: string
    - jsonPath: .spec.modelVersion
      name: Version
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    name: v1
    schema:
      openAPIV3Schema:
        description: InferenceService is the Schema for the inferenceservices API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of InferenceService
            properties:
              activeDeadline:
                format: int64
                minimum: 0
                type: integer
              autoscaling:
                description: 自动扩缩容配置
                properties:
                  maxReplicas:
                    format: int32
                    minimum: 1
                    type: integer
                  minReplicas:
                    format: int32
                    minimum: 1
                    type: integer
                  targetCPUUtilization:
                    description: 目标 CPU 使用率 (0-100)
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                  targetMemoryUtilization:
                    description: 目标内存使用率 (0-100)
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                required:
                - maxReplicas
                - targetCPUUtilization
                - targetMemoryUtilization
                type: object
              canary:
                description: 灰度发布配置
                properties:
                  enabled:
                    type: boolean
                  image:
                    pattern: ^([a-zA-Z0-9-]+(\.[a-zA-Z0-9-]+)*(:[a-zA-Z0-9._-]+)?|([a-zA-Z0-9-]+\/)?[a-zA-Z0-9._-]+(:[a-zA-Z0-9._-]+)?|@sha256:[0-9a-f]{64})$
                    type: string
                  modelName:
                    description: 灰度模型名（通常与主模型相同）
                    minLength: 1
                    type: string
                  modelVersion:
                    description: 灰度模型版本
                    minLength: 1
                    type: string
                  resources:
                    description: ResourceRequirements describes the compute resource
                      requirements.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  version:
                    description: 灰度版本号
                    minLength: 1
                    type: string
                  weight:
                    description: 流量比例 0-100
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                required:
                - enabled
                - modelName
                - modelVersion
                - version
                - weight
                type: object
              image:
                description: 镜像信息(如果模型是自定义镜像,否则用默认推理镜像+模型挂载)
                pattern: ^([a-zA-Z0-9-]+(\.[a-zA-Z0-9-]+)*(:[a-zA-Z0-9._-]+)?|([a-zA-Z0-9-]+\/)?[a-zA-Z0-9._-]+(:[a-zA-Z0-9._-]+)?|@sha256:[0-9a-f]{64})$
                type: string
              modelName:
                description: 模型信息
                minLength: 1
                type: string
              modelPath:
                pattern: ^\/?[a-zA-Z0-9_\-\/]+$
                type: string
              modelVersion:
                minLength: 1
                type: string
              nodeName:
                description: 节点名称（可被HPA覆盖）
                type: string
              port:
                default: 8501
                description: 容器端口 (默认 8501)
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              replicas:
                description: 副本数（可被HPA覆盖）
                format: int32
                minimum: 1
                type: integer
              resources:
                description: 资源限制
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              service:
                description: 服务暴露
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: 可用于Ingress配置
                    type: object
                  host:
                    description: Ingress 域名 (如果不填，则不创建 Ingress)
                    pattern: ^([a-zA-Z0-9]([a-zA-Z0-9-]*[a-zA-Z0-9])?\.)+[a-zA-Z]{2,}$
                    type: string
                  port:
                    description: 服务端口
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  targetPort:
                    description: 容器端口
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  type:
                    description: ClusterIp, NodePort, LoadBalancer
                    enum:
                    - ClusterIP
                    - NodePort
                    - LoadBalancer
                    type: string
                required:
                - port
                - targetPort
                - type
                type: object
            required:
            - modelName
            - modelVersion
            - resources
            - service
            type: object
          status:
            description: status defines the observed state of InferenceService
            properties:
              canaryState:
                description: 灰度版本部署状态(如有)
                enum:
                - Pending
                - Running
                - Failed
                - Succeeded
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastUpdateTime:
                description: 最后更新时间
                format: date-time
                type: string
              ready:
                description: 服务是否就绪
                type: boolean
              readyReplicas:
                description: 当前副本数
                format: int32
                minimum: 0
                type: integer
              registeredModel:
                type: string
              stableState:
                description: 稳定版本部署状态
                enum:
                - Pending
                - Running
                - Failed
                - Succeeded
                type: string
              url:
                description: 服务访问地址
                type: string
            type: object
        required:
        - spec
        type: object
        x-kubernetes-validations:
        - message: maxReplicas must be greater than or equal to minReplicas in autoscaling
          rule: '!has(self.spec.autoscaling) || (self.spec.autoscaling.minReplicas
            == nil || self.spec.autoscaling.maxReplicas >= self.spec.autoscaling.minReplicas)'
        - message: canary weight must be between 0 and 100 when canary is enabled
          rule: '!has(self.spec.canary) || !self.spec.canary.enabled || (self.spec.canary.weight
            >=0 && self.spec.canary.weight <=100)'
    served: true
    storage: true
    subresources:
      status: {}
