apiVersion: v1
kind: Service
metadata:
  name: inference-gateway
  namespace: kubeai
  labels:
    app: kubeai-inference-gateway
spec:
  type: ClusterIP
  ports:
    - port: 58082
      targetPort: 58082
      protocol: TCP
      name: http
  selector:
    app: kubeai-inference-gateway
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inference-gateway
  namespace: kubeai
  labels:
    app: inference-gateway
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kubeai-inference-gateway
  template:
    metadata:
      labels:
        app: kubeai-inference-gateway
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "58082"
        prometheus.io/scheme: "http"
        prometheus.io/path: "/api/v1/inference/metrics"
    spec:
      containers:
        - name: inference-gateway
          image: docker.io/your-registry/kubeai-inference-gateway:v1.0.0
          ports:
            - containerPort: 58082
          env:
            - name: DB_HOST
              value: "postgres.kubeai.svc.cluster.local"
            - name: DB_PORT
              value: "5432"
            - name: DB_USER
              value: "postgres"
            - name: DB_PASSWORD
              value: "postgres"
            - name: DB_NAME
              value: "kubeai_platform"
            - name: MINIO_ENDPOINT
              value: "minio.kubeai.svc.cluster.local:9000"
            - name: MINIO_ACCESS_KEY
              value: "minioadmin"
            - name: MINIO_SECRET_KEY
              value: "minioadmin"
            - name: MINIO_BUCKET
              value: "models"
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /api/v1/inference/health
              port: 58082
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /api/v1/inference/ready
              port: 58082
            initialDelaySeconds: 5
            periodSeconds: 10